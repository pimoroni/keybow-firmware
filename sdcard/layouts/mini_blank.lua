require "keybow"

-- Key mappings for Keybow MINI --

function handle_minikey_00(pressed)
    keybow.set_key("", pressed)
end

function handle_minikey_01(pressed)
    keybow.set_key("", pressed)
end

function handle_minikey_02(pressed)
    keybow.set_key("", pressed)
end
